# 研途总控台 — 数据库设计文档

> 最后更新：2026-06-14 | 数据库引擎：SQLite (better-sqlite3, WAL 模式)

---

## ER 关系概览

```
users (1) ──< tasks (N)          users (1) ──< summaries (N)
users (1) ──< courses (N)        users (1) ──< live_sessions (N)
users (1) ──< forum_topics (N)   users (1) ──< forum_replies (N)
users (1) ──< questions (N)      users (1) ──< practice_records (N)
users (1) ──< products (N)       users (1) ──< orders (N)
users (1) ──< notifications (N)  users (1) ──< auth_tokens (N)
users (1) ──< flashcards (N)     users (1) ──< flashcard_records (N)
users (1) ──< practice_sessions (N)

tasks (1) ──< task_completions (N)
tasks (1) ──< notifications (N)
live_sessions (1) ──< live_messages (N)
forum_topics (1) ──< forum_replies (N)
questions <>─ question_tags (多对多, via question_tag_relations)
folders (1) ──< folders (N) [自引用]
folders (1) ──< folder_items (N)
flashcards (1) ──< flashcard_records (N)
```

---

## 表结构详情

### users — 用户表

| 列 | 类型 | 约束 | 说明 |
|---|---|---|---|
| id | INTEGER | PK, AUTO | 主键 |
| username | TEXT | NOT NULL, UNIQUE | 用户名 |
| password | TEXT | NOT NULL | bcrypt 哈希密码 |
| role | TEXT | NOT NULL, CHECK IN ('admin','teacher','student') | 角色 |
| display_name | TEXT | NOT NULL | 显示名称 |
| class_name | TEXT | DEFAULT '' | 班级名称 |
| openid | TEXT | DEFAULT '' | 微信 openid |
| created_at | TEXT | NOT NULL | 创建时间 ISO |

### teacher_applications — 教师注册申请

| 列 | 类型 | 约束 | 说明 |
|---|---|---|---|
| id | INTEGER | PK, AUTO | 主键 |
| username | TEXT | NOT NULL, UNIQUE | 申请用户名 |
| password | TEXT | NOT NULL | bcrypt 哈希密码 |
| display_name | TEXT | NOT NULL | 显示名称 |
| class_name | TEXT | DEFAULT '' | 班级 |
| motivation | TEXT | DEFAULT '' | 申请说明 |
| status | TEXT | CHECK IN ('pending','approved','rejected') | 状态 |
| reviewed_by | INTEGER | FK → users.id | 审核人 |
| reviewed_at | TEXT | | 审核时间 |
| created_at | TEXT | NOT NULL | 申请时间 |

### tasks — 任务表

| 列 | 类型 | 约束 | 说明 |
|---|---|---|---|
| id | INTEGER | PK, AUTO | 主键 |
| title | TEXT | NOT NULL | 任务标题 |
| description | TEXT | DEFAULT '' | 任务描述 |
| subject | TEXT | DEFAULT '考研规划' | 科目 |
| start_time | TEXT | NOT NULL | 开始时间 HH:mm |
| end_time | TEXT | NOT NULL | 结束时间 HH:mm |
| weekdays | TEXT | NOT NULL | JSON 数组 [0-6]，0=周日 |
| student_ids | TEXT | NOT NULL | JSON 数组 |
| created_by | INTEGER | FK → users.id | 创建教师 |
| created_at | TEXT | NOT NULL | 创建时间 |

### task_completions — 任务完成记录

| 列 | 类型 | 约束 | 说明 |
|---|---|---|---|
| id | INTEGER | PK, AUTO | 主键 |
| task_id | INTEGER | FK → tasks.id | 任务 ID |
| student_id | INTEGER | FK → users.id | 学生 ID |
| task_date | TEXT | NOT NULL | 日期 YYYY-MM-DD |
| completed_at | TEXT | | 完成时间 |
| | | UNIQUE(task_id, student_id, task_date) | 唯一约束 |

### summaries — 学习总结

| 列 | 类型 | 约束 | 说明 |
|---|---|---|---|
| id | INTEGER | PK, AUTO | 主键 |
| student_id | INTEGER | FK → users.id | 学生 |
| task_date | TEXT | NOT NULL | 日期 |
| content | TEXT | DEFAULT '' | 文字内容 |
| image_paths | TEXT | DEFAULT '[]' | JSON 图片路径数组 |
| attachment_paths | TEXT | DEFAULT '[]' | JSON 附件路径数组 |
| created_at | TEXT | NOT NULL | |
| updated_at | TEXT | NOT NULL | |
| | | UNIQUE(student_id, task_date) | |

### notifications — 通知

| 列 | 类型 | 约束 | 说明 |
|---|---|---|---|
| id | INTEGER | PK, AUTO | |
| student_id | INTEGER | FK → users.id | |
| type | TEXT | NOT NULL | 类型：每日任务/学习提醒/晚间提醒 |
| title | TEXT | NOT NULL | 标题 |
| body | TEXT | NOT NULL | 内容 |
| task_id | INTEGER | FK → tasks.id, NULL | 关联任务 |
| task_date | TEXT | DEFAULT '' | |
| schedule_key | TEXT | UNIQUE | 去重键 |
| read_at | TEXT | NULL | 已读时间 |
| created_at | TEXT | NOT NULL | |

### auth_tokens — 认证 Token

| 列 | 类型 | 约束 | 说明 |
|---|---|---|---|
| token | TEXT | PK | 随机 Token |
| user_id | INTEGER | FK → users.id | |
| created_at | TEXT | NOT NULL | |
| expires_at | TEXT | NOT NULL | 过期时间 |

### folders — 文件夹（自引用树）

| 列 | 类型 | 约束 | 说明 |
|---|---|---|---|
| id | INTEGER | PK, AUTO | |
| name | TEXT | NOT NULL | 文件夹名 |
| parent_id | INTEGER | FK → folders.id, ON DELETE CASCADE | 父文件夹 |
| created_by | INTEGER | FK → users.id | |
| created_at | TEXT | NOT NULL | |

**索引：** `idx_folders_parent ON folders(parent_id)`

### folder_items — 文件项

| 列 | 类型 | 约束 | 说明 |
|---|---|---|---|
| id | INTEGER | PK, AUTO | |
| folder_id | INTEGER | FK → folders.id, ON DELETE CASCADE | |
| item_type | TEXT | CHECK IN ('course','file','video') | 类型 |
| title | TEXT | NOT NULL | |
| description | TEXT | DEFAULT '' | |
| subject | TEXT | DEFAULT '' | |
| file_path | TEXT | DEFAULT '' | 上传文件路径 |
| file_url | TEXT | DEFAULT '' | 外部 URL |
| file_size | INTEGER | DEFAULT 0 | 字节 |
| sort_order | INTEGER | DEFAULT 0 | |
| created_by | INTEGER | FK → users.id | |
| created_at | TEXT | NOT NULL | |

### forum_topics — 论坛帖子

| 列 | 类型 | 约束 | 说明 |
|---|---|---|---|
| id | INTEGER | PK, AUTO | |
| user_id | INTEGER | FK → users.id | |
| title | TEXT | NOT NULL | |
| content | TEXT | NOT NULL | |
| category | TEXT | DEFAULT '考研交流' | 分类 |
| image_paths | TEXT | DEFAULT '[]' | JSON 图片路径 |
| attachment_paths | TEXT | DEFAULT '[]' | JSON 附件路径 |
| video_paths | TEXT | DEFAULT '[]' | JSON 视频路径 |
| links | TEXT | DEFAULT '[]' | JSON 链接数组 |
| created_at | TEXT | NOT NULL | |

### forum_replies — 论坛回复

同 forum_topics 结构（不含 title 和 category），增加 topic_id 外键。

### questions — 题目

| 列 | 类型 | 约束 | 说明 |
|---|---|---|---|
| id | INTEGER | PK, AUTO | |
| title | TEXT | NOT NULL | 标题 |
| subject | TEXT | DEFAULT '考研英语' | 科目 |
| question_type | TEXT | DEFAULT '' | 题型 |
| textbook | TEXT | DEFAULT '' | 参考书 |
| stem | TEXT | NOT NULL | 题干 |
| options | TEXT | NOT NULL | JSON 选项数组 |
| correct_answer | TEXT | NOT NULL | 正确答案 |
| analysis_text | TEXT | DEFAULT '' | 文字解析 |
| analysis_video_path | TEXT | DEFAULT '' | 解析视频路径 |
| analysis_video_url | TEXT | DEFAULT '' | 解析视频 URL |
| created_by | INTEGER | FK → users.id | |
| created_at | TEXT | NOT NULL | |

### question_tags — 题目标签

| 列 | 类型 | 约束 | 说明 |
|---|---|---|---|
| id | INTEGER | PK, AUTO | |
| name | TEXT | NOT NULL, UNIQUE | 标签名 |
| category | TEXT | CHECK IN ('subject','type','textbook','custom') | 分类 |
| created_at | TEXT | NOT NULL | |

### question_tag_relations — 题目标签关联

| 列 | 类型 | 约束 | 说明 |
|---|---|---|---|
| question_id | INTEGER | FK → questions.id, ON DELETE CASCADE | |
| tag_id | INTEGER | FK → question_tags.id, ON DELETE CASCADE | |
| | | PK(question_id, tag_id) | 联合主键 |

### flashcards — 词汇卡片

| 列 | 类型 | 约束 | 说明 |
|---|---|---|---|
| id | INTEGER | PK, AUTO | |
| title | TEXT | NOT NULL | 单词/标题 |
| subject | TEXT | DEFAULT '' | 科目 |
| front_content | TEXT | NOT NULL | 正面内容 |
| front_image_path | TEXT | DEFAULT '' | 正面图片 |
| back_content | TEXT | NOT NULL | 背面内容 |
| back_image_path | TEXT | DEFAULT '' | 背面图片 |
| audio_path | TEXT | DEFAULT '' | 音频路径 |
| example_sentence | TEXT | DEFAULT '' | 例句 |
| word_root | TEXT | DEFAULT '' | 词根 |
| affix | TEXT | DEFAULT '' | 词缀 |
| collocations | TEXT | DEFAULT '' | 常见搭配 |
| phonetic | TEXT | DEFAULT '' | 音标 |
| tags | TEXT | DEFAULT '[]' | JSON 标签数组 |
| created_by | INTEGER | FK → users.id | |
| created_at | TEXT | NOT NULL | |

### flashcard_records — 词汇复习记录

| 列 | 类型 | 约束 | 说明 |
|---|---|---|---|
| id | INTEGER | PK, AUTO | |
| flashcard_id | INTEGER | FK → flashcards.id | |
| student_id | INTEGER | FK → users.id | |
| quality | INTEGER | NOT NULL, DEFAULT 0 | 评分 0-3 |
| ease_factor | REAL | NOT NULL, DEFAULT 2.5 | SM-2 难度因子 |
| interval_days | INTEGER | NOT NULL, DEFAULT 0 | 间隔天数 |
| repetitions | INTEGER | NOT NULL, DEFAULT 0 | 连续正确次数 |
| next_review_date | TEXT | NOT NULL | 下次复习日期 |
| created_at | TEXT | NOT NULL | |
| | | UNIQUE(flashcard_id, student_id) | |

### practice_sessions — 练习会话

| 列 | 类型 | 约束 | 说明 |
|---|---|---|---|
| id | TEXT | PK | UUID |
| student_id | INTEGER | FK → users.id | |
| session_type | TEXT | CHECK IN ('mixed','subject','flashcard','wrong_review') | |
| subject_filter | TEXT | DEFAULT '' | |
| total_questions | INTEGER | DEFAULT 0 | |
| correct_count | INTEGER | DEFAULT 0 | |
| started_at | TEXT | NOT NULL | |
| ended_at | TEXT | NULL | |

### practice_records — 答题记录

| 列 | 类型 | 约束 | 说明 |
|---|---|---|---|
| id | INTEGER | PK, AUTO | |
| question_id | INTEGER | FK → questions.id | |
| student_id | INTEGER | FK → users.id | |
| selected_answer | TEXT | NOT NULL | 学生答案 |
| is_correct | INTEGER | NOT NULL | 是否正确 |
| time_spent_ms | INTEGER | DEFAULT 0 | 答题用时 |
| session_id | TEXT | DEFAULT '' | 所属练习会话 |
| created_at | TEXT | NOT NULL | |

### courses — 课程（保留兼容）

| 列 | 类型 | 约束 | 说明 |
|---|---|---|---|
| id | INTEGER | PK, AUTO | |
| title | TEXT | NOT NULL | |
| description | TEXT | DEFAULT '' | |
| subject | TEXT | DEFAULT '考研规划' | |
| video_path | TEXT | DEFAULT '' | |
| video_url | TEXT | DEFAULT '' | |
| created_by | INTEGER | FK → users.id | |
| created_at | TEXT | NOT NULL | |

### products — 商品

| 列 | 类型 | 约束 | 说明 |
|---|---|---|---|
| id | INTEGER | PK, AUTO | |
| title | TEXT | NOT NULL | 商品标题 |
| description | TEXT | DEFAULT '' | 描述 |
| price | REAL | NOT NULL | 售价 |
| original_price | REAL | DEFAULT 0 | 原价 |
| stock | INTEGER | DEFAULT 0 | 库存 |
| category | TEXT | DEFAULT '' | 分类 |
| is_virtual | INTEGER | DEFAULT 0 | 是否虚拟商品 |
| virtual_content | TEXT | DEFAULT '' | 虚拟商品内容/链接 |
| image_path | TEXT | DEFAULT '' | 商品图片 |
| created_by | INTEGER | FK → users.id | 教师 |
| created_at | TEXT | NOT NULL | |

### orders — 订单

| 列 | 类型 | 约束 | 说明 |
|---|---|---|---|
| id | INTEGER | PK, AUTO | |
| product_id | INTEGER | FK → products.id, ON DELETE SET NULL | 商品 |
| student_id | INTEGER | FK → users.id | 学生 |
| quantity | INTEGER | NOT NULL | 数量 |
| total_amount | REAL | NOT NULL | 总价 |
| shipping_address | TEXT | NOT NULL | 收货地址 |
| status | TEXT | DEFAULT 'paid' | paid/shipped/delivered/confirmed/cancelled |
| created_at | TEXT | NOT NULL | |

### live_sessions — 直播会话

| 列 | 类型 | 约束 | 说明 |
|---|---|---|---|
| id | INTEGER | PK, AUTO | |
| title | TEXT | NOT NULL | 直播标题 |
| description | TEXT | DEFAULT '' | 描述 |
| teacher_id | INTEGER | FK → users.id | 教师 |
| status | TEXT | DEFAULT 'pending' | pending/live/ended |
| started_at | TEXT | NULL | 开始时间 |
| ended_at | TEXT | NULL | 结束时间 |
| created_at | TEXT | NOT NULL | |

### live_messages — 直播聊天消息

| 列 | 类型 | 约束 | 说明 |
|---|---|---|---|
| id | INTEGER | PK, AUTO | |
| live_session_id | INTEGER | FK → live_sessions.id | 直播间 |
| user_id | INTEGER | FK → users.id | 发送者 |
| content | TEXT | NOT NULL | 内容 |
| created_at | TEXT | NOT NULL | |

---

## 新增业务表（v2.1.0 补充）

以下表为 v2.0.0 之后新增，用于支持商城、课程、社交、AI、习惯追踪等功能。

### course_notes — 课程笔记

| 列 | 类型 | 约束 | 说明 |
|---|---|---|---|
| id | INTEGER | PK, AUTO | |
| item_id | INTEGER | FK → folder_items.id, ON DELETE CASCADE | 课程文件项 |
| student_id | INTEGER | FK → users.id | 学生 |
| content | TEXT | NOT NULL | 笔记内容 |
| timestamp_seconds | REAL | DEFAULT 0 | 视频时间戳 |
| created_at | TEXT | NOT NULL | |

### course_reviews — 课程评价

| 列 | 类型 | 约束 | 说明 |
|---|---|---|---|
| id | INTEGER | PK, AUTO | |
| item_id | INTEGER | FK → folder_items.id, ON DELETE CASCADE | 课程文件项 |
| student_id | INTEGER | FK → users.id | 学生 |
| rating | INTEGER | CHECK 1-5 | 评分 |
| content | TEXT | DEFAULT '' | 评价内容 |
| created_at | TEXT | NOT NULL | |
| | | UNIQUE(item_id, student_id) | |

### shopping_cart — 购物车

| 列 | 类型 | 约束 | 说明 |
|---|---|---|---|
| id | INTEGER | PK, AUTO | |
| student_id | INTEGER | FK → users.id | 学生 |
| product_id | INTEGER | FK → products.id, ON DELETE CASCADE | 商品 |
| quantity | INTEGER | DEFAULT 1 | 数量 |
| created_at | TEXT | NOT NULL | |
| | | UNIQUE(student_id, product_id) | |

### address_book — 地址簿

| 列 | 类型 | 约束 | 说明 |
|---|---|---|---|
| id | INTEGER | PK, AUTO | |
| student_id | INTEGER | FK → users.id | 学生 |
| name | TEXT | NOT NULL | 收件人 |
| phone | TEXT | DEFAULT '' | 电话 |
| address | TEXT | NOT NULL | 地址 |
| is_default | INTEGER | DEFAULT 0 | 是否默认 |
| created_at | TEXT | NOT NULL | |

### product_reviews — 商品评价

| 列 | 类型 | 约束 | 说明 |
|---|---|---|---|
| id | INTEGER | PK, AUTO | |
| product_id | INTEGER | FK → products.id | 商品 |
| student_id | INTEGER | FK → users.id | 学生 |
| rating | INTEGER | CHECK 1-5 | 评分 |
| content | TEXT | DEFAULT '' | 内容 |
| created_at | TEXT | NOT NULL | |

### live_reservations — 直播预约

| 列 | 类型 | 约束 | 说明 |
|---|---|---|---|
| id | INTEGER | PK, AUTO | |
| live_session_id | INTEGER | FK → live_sessions.id | 直播间 |
| student_id | INTEGER | FK → users.id | 学生 |
| created_at | TEXT | NOT NULL | |
| | | UNIQUE(live_session_id, student_id) | |

### live_polls / live_poll_votes — 直播投票

| 表 | 列 | 类型 | 约束 | 说明 |
|---|---|---|---|---|
| live_polls | id | INTEGER | PK, AUTO | |
| live_polls | live_session_id | INTEGER | FK → live_sessions.id | 直播间 |
| live_polls | question | TEXT | NOT NULL | 问题 |
| live_polls | options | TEXT | NOT NULL | JSON 选项数组 |
| live_polls | created_at | TEXT | NOT NULL | |
| live_poll_votes | poll_id | INTEGER | FK → live_polls.id | 投票 |
| live_poll_votes | user_id | INTEGER | FK → users.id | 用户 |
| live_poll_votes | option_index | INTEGER | NOT NULL | 选项索引 |
| live_poll_votes | created_at | TEXT | NOT NULL | |
| live_poll_votes | | | UNIQUE(poll_id, user_id) | |

### question_notes — 题目笔记

| 列 | 类型 | 约束 | 说明 |
|---|---|---|---|
| id | INTEGER | PK, AUTO | |
| question_id | INTEGER | FK → questions.id | 题目 |
| student_id | INTEGER | FK → users.id | 学生 |
| content | TEXT | NOT NULL | 笔记内容 |
| created_at | TEXT | NOT NULL | |
| updated_at | TEXT | NOT NULL | |
| | | UNIQUE(question_id, student_id) | |

### wrong_review_schedule — 错题复习计划

| 列 | 类型 | 约束 | 说明 |
|---|---|---|---|
| id | INTEGER | PK, AUTO | |
| question_id | INTEGER | FK → questions.id | 题目 |
| student_id | INTEGER | FK → users.id | 学生 |
| review_stage | INTEGER | DEFAULT 0 | 复习阶段 |
| next_review_date | TEXT | NOT NULL | 下次复习日期 |
| reviewed_count | INTEGER | DEFAULT 0 | 已复习次数 |
| completed_at | TEXT | NULL | 完成时间 |
| created_at | TEXT | NOT NULL | |

### mock_exams / mock_exam_submissions — 模拟考试

| 表 | 列 | 类型 | 约束 | 说明 |
|---|---|---|---|---|
| mock_exams | id | INTEGER | PK, AUTO | |
| mock_exams | title | TEXT | NOT NULL | 试卷标题 |
| mock_exams | teacher_id | INTEGER | FK → users.id | 创建教师 |
| mock_exams | question_ids | TEXT | NOT NULL | JSON 题目 ID 数组 |
| mock_exams | duration_minutes | INTEGER | NOT NULL | 考试时长 |
| mock_exams | created_at | TEXT | NOT NULL | |
| mock_exam_submissions | id | INTEGER | PK, AUTO | |
| mock_exam_submissions | mock_exam_id | INTEGER | FK → mock_exams.id | 试卷 |
| mock_exam_submissions | student_id | INTEGER | FK → users.id | 学生 |
| mock_exam_submissions | answers | TEXT | NOT NULL | JSON 答题结果 |
| mock_exam_submissions | score | INTEGER | DEFAULT 0 | 得分 |
| mock_exam_submissions | started_at | TEXT | NOT NULL | |
| mock_exam_submissions | submitted_at | TEXT | NULL | 交卷时间 |

### group_buys / group_buy_participants — 拼团

| 表 | 列 | 类型 | 约束 | 说明 |
|---|---|---|---|---|
| group_buys | id | INTEGER | PK, AUTO | |
| group_buys | product_id | INTEGER | FK → products.id | 商品 |
| group_buys | creator_id | INTEGER | FK → users.id | 团长 |
| group_buys | required_count | INTEGER | NOT NULL | 成团人数 |
| group_buys | current_count | INTEGER | DEFAULT 1 | 当前人数 |
| group_buys | status | TEXT | DEFAULT 'open' | open/closed/expired |
| group_buys | created_at | TEXT | NOT NULL | |
| group_buy_participants | id | INTEGER | PK, AUTO | |
| group_buy_participants | group_buy_id | INTEGER | FK → group_buys.id | 拼团 |
| group_buy_participants | student_id | INTEGER | FK → users.id | 学生 |
| group_buy_participants | created_at | TEXT | NOT NULL | |
| group_buy_participants | | | UNIQUE(group_buy_id, student_id) | |

### exam_countdown — 考研倒计时

| 列 | 类型 | 约束 | 说明 |
|---|---|---|---|
| id | INTEGER | PK, AUTO | |
| student_id | INTEGER | FK → users.id | 学生 |
| exam_date | TEXT | NOT NULL | 考试日期 |
| created_at | TEXT | NOT NULL | |
| updated_at | TEXT | NOT NULL | |
| | | UNIQUE(student_id) | |

### habit_tracking — 习惯追踪

| 列 | 类型 | 约束 | 说明 |
|---|---|---|---|
| id | INTEGER | PK, AUTO | |
| student_id | INTEGER | FK → users.id | 学生 |
| title | TEXT | NOT NULL | 习惯名称 |
| color | TEXT | DEFAULT '' | 颜色标识 |
| created_at | TEXT | NOT NULL | |

### habit_checkins — 习惯打卡

| 列 | 类型 | 约束 | 说明 |
|---|---|---|---|
| id | INTEGER | PK, AUTO | |
| habit_id | INTEGER | FK → habit_tracking.id, ON DELETE CASCADE | 习惯 |
| student_id | INTEGER | FK → users.id | 学生 |
| checkin_date | TEXT | NOT NULL | 打卡日期 |
| created_at | TEXT | NOT NULL | |
| | | UNIQUE(habit_id, checkin_date) | |

### ai_conversations — AI 对话记录

| 列 | 类型 | 约束 | 说明 |
|---|---|---|---|
| id | INTEGER | PK, AUTO | |
| student_id | INTEGER | FK → users.id | 学生 |
| role | TEXT | NOT NULL | user / assistant |
| content | TEXT | NOT NULL | 内容 |
| created_at | TEXT | NOT NULL | |

### follows — 关注关系

| 列 | 类型 | 约束 | 说明 |
|---|---|---|---|
| follower_id | INTEGER | FK → users.id | 关注者 |
| following_id | INTEGER | FK → users.id | 被关注者 |
| created_at | TEXT | NOT NULL | |
| | | PK(follower_id, following_id) | 联合主键 |

### reports — 内容举报

| 列 | 类型 | 约束 | 说明 |
|---|---|---|---|
| id | INTEGER | PK, AUTO | |
| reporter_id | INTEGER | FK → users.id | 举报人 |
| target_type | TEXT | NOT NULL | topic / reply / user |
| target_id | INTEGER | NOT NULL | 目标 ID |
| reason | TEXT | NOT NULL | 举报原因 |
| status | TEXT | DEFAULT 'pending' | pending/reviewed/rejected |
| reviewed_by | INTEGER | FK → users.id, NULL | 审核人 |
| reviewed_at | TEXT | NULL | 审核时间 |
| created_at | TEXT | NOT NULL | |

### forum_trending — 论坛热门话题

| 列 | 类型 | 约束 | 说明 |
|---|---|---|---|
| id | INTEGER | PK, AUTO | |
| topic_id | INTEGER | FK → forum_topics.id, ON DELETE CASCADE | 帖子 |
| score | REAL | DEFAULT 0 | 热度分 |
| calculated_at | TEXT | NOT NULL | 计算时间 |
| | | UNIQUE(topic_id) | |

### search_logs — 搜索日志

| 列 | 类型 | 约束 | 说明 |
|---|---|---|---|
| id | INTEGER | PK, AUTO | |
| keyword | TEXT | NOT NULL | 关键词 |
| student_id | INTEGER | FK → users.id, NULL | 学生 |
| created_at | TEXT | NOT NULL | |

### study_streaks — 学习连续天数

| 列 | 类型 | 约束 | 说明 |
|---|---|---|---|
| student_id | INTEGER | PK, FK → users.id | 学生 |
| current_streak | INTEGER | DEFAULT 0 | 当前连续天数 |
| last_study_date | TEXT | NULL | 最后学习日期 |
| updated_at | TEXT | NOT NULL | |

---

> 最后更新：2026-06-14 | 数据库引擎：SQLite (better-sqlite3, WAL 模式)
