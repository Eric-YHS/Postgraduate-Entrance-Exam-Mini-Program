# 技术要求落地验收

验收日期：2026-07-25

## 结论

`技术要求/技术要求` 中可读取的业务要求已落地到现有服务端、管理后台、公开登记页和微信小程序。最终回归结果如下：

| 检查项 | 结果 |
| --- | --- |
| 服务端 Jest | 29 个套件、150 项测试全部通过 |
| 小程序 TypeScript | `npm run typecheck` 通过 |
| 小程序发布模式 | `npm run verify:release` 通过，模式为 `restricted` |
| Git 差异检查 | `git diff --check` 通过 |
| 浏览器控制台 | 管理后台、公开登记页均为 0 error / 0 warning |
| 浏览器网络 | 本次验收涉及的 API 均返回 200 |
| 响应式 | 1440x900 桌面端与 390x844 移动端通过；移动端页面无横向溢出 |

本地已配置并通过微信官方 `access_token` 接口验证小程序 AppID、AppSecret 和订阅消息模板；MiniMax-M3 与 DeepSeek 也分别通过最小真实模型请求验证。企业微信基础鉴权有效，并已从生产服务器可信出口读取应用信息和 1 个微信客服账号；微信客服已启用。服务器现有的会话存档专用 Secret、RSA 私钥、原生 SDK、`SESSION_SECRET` 和火山引擎语音参数也已核对并同步到本地私密 `.env`。`npm start`/`npm run dev` 会自动读取 `.env`，生产 PM2 也通过 `env_file` 使用同一配置。目前仅 SMTP 投递凭据未在本地或服务器配置中找到，邮件功能会在缺少 SMTP 时明确记录失败状态；其余代码、配置入口、协议适配、失败处理和自动化测试已经具备。

## 输入清单

| 原始文件 | 处理结论 |
| --- | --- |
| `技术要求.docx` | 5 类主需求全部落地，见下表 |
| `自动回复机器人配置手册.docx` | 第 1-15 节及文档内全部配置表已转为可校验配置、运行时行为和运营界面 |
| `表1.xls` | 16 列学员档案字段已落地 |
| `表2.xls` | 11 列目标分数与分科任务模板已落地 |
| `表3.xls` | 晚间分科完成情况登记、解析和次日调整已落地 |
| `表4.xls` | 原始二进制 XLS 可直接导入单词与知识库，样例 `difficult` 已验收 |
| `表5.xls` | 原文件只有表头、没有题目数据；空行会被安全忽略，完整题目行的导入契约已测试 |
| V5/V9 管理后台 HTML | 机器人、Prompt、关键词、模板、推送、工单、审计和灰度等信息架构已融合进现有 `/admin`，没有另建演示页面 |
| `机器人.docx` | 原文件大小为 0 字节，不包含可执行要求 |

## 主需求矩阵

| 编号 | 原始要求 | 落地内容 | 主要实现 | 验收证据 |
| --- | --- | --- | --- | --- |
| 1 | 学生信息登记（表 1）；企微首次打招呼发送链接；小程序不显示 | 管理员可按站内学生 ID 或企微 UserID 生成带随机 token 的公开登记链接；配置机器人首次接触时自动附带链接；公开页覆盖表 1 字段；小程序路由不包含登记页 | `src/routes/requirements.js`、`src/services/configuredBotRuntime.js`、`public/student-profile.html`、`public/student-profile.js` | `tests/requirements.test.js`；`output/playwright/student-profile-submitted.yml`；桌面/移动截图 |
| 2 | 半自动/全自动复习规划（表 2）；早发计划、晚登记（表 3）；小程序显示未来 7 天 | 持久化表 2 模板；手工、半自动和全自动计划；晚间按编号登记完成情况；未完成任务回炉；每天早晚定时任务；学生接口只取指定日期起的 7 天 | `src/services/studyPlan.js`、`src/routes/requirements.js`、`src/services/bots/supervisorBot.js`、`src/services/scheduler.js`、`miniprogram/pages/plan/` | `tests/requirements.test.js`、`tests/supervisorRequirements.test.js`；`output/playwright/admin-plan-template-desktop.png`；半/全自动快照 |
| 3 | 自建知识库；上传书本、表 4 单词、表 5 题目；先检索再走模型；小程序有单词和政治选择题 | 知识库/文档/分块持久化；支持 PDF、DOCX、XLS/XLSX、CSV、TXT、Markdown；中文 bigram 检索；RAG 注入；表 4/表 5 专用导入；小程序新增单词复习和题库契约 | `src/routes/knowledgeBase.js`、`src/services/knowledgeBase.js`、`src/routes/questions.js`、`miniprogram/pages/vocabulary/` | `tests/knowledgeBase.test.js`、`tests/requirements.test.js`；知识库处理与导入浏览器快照 |
| 4 | 按本周学习情况生成美观总结并直接发邮件；小程序不显示 | 生成转义后的 HTML 周报，包含概览、分科、完成率和建议；SMTP 投递；投递结果持久化；缺配置或失败时明确记录状态；未加入小程序页面 | `src/services/reportMailer.js`、`src/services/scheduler.js`、`src/db.js` | `tests/reportMailer.test.js` |
| 5 | 机器人创建页面直接呈现；小程序不显示 | 管理后台提供机器人创建、编辑、检查、上线、暂停、删除、群/客服分配、手动推送、审计、灰度和运营面板；教师无管理权限；小程序无机器人配置路由 | `public/admin.html`、`public/admin.js`、`src/routes/bots.js`、`src/routes/wecomAdmin.js`、`src/routes/wecomKfAdmin.js` | `tests/botsRequirements.test.js`、`tests/wecomAdmin.test.js`；机器人、审计和运营浏览器证据 |

## 配置手册 1-15 节

| 节 | 要求 | 落地与强制规则 | 自动化证据 |
| --- | --- | --- | --- |
| 1 | 基础信息 7 字段；机器人 ID 按 R-xx 递增 | `robot_uid` 独立唯一并自动分配；名称、昵称、头像、类型/定位、说明均在配置页持久化；历史机器人迁移时补齐 R-xx | `tests/databaseMigration.test.js`、`tests/botsRequirements.test.js` |
| 2 | 说话风格 5 维度和自定义人设 | 口吻、称谓、自称、收尾、禁用话术进入 `style` 配置；上线检查拒绝廉价称呼、空泛鼓励、emoji 堆砌和结构化标签 | `tests/robotConfig.test.js` |
| 3 | Prompt 必须为角色/上下文/任务/输出规范四段式 | 上线前逐条检查 4 个标题；运行时读取学员姓名、目标专业、阶段、错题、打卡率和班主任上下文并构建系统提示词 | `tests/robotConfig.test.js`、`src/services/contextBuilder.js` |
| 4 | 系统红线、自定义禁用词和三级违规处理 | 内置绝对化承诺、违规资料等红线；支持自定义词；输入命中立即转人工并告警，输出命中替换兜底；1 小时第 3 次创建电话升级和员工通知 | `tests/botsRequirements.test.js` |
| 5 | 每机器人至少 5 条专属语料，先 RAG 再回答 | 上线门禁要求 5+ 条；按中文和英文关键词评分检索；知识库结果与机器人专属语料共同注入 | `tests/robotConfig.test.js`、`tests/knowledgeBase.test.js` |
| 6 | 精确/包含/正则；P0>P1>P2；转人工/业务/兜底三类 | 三种匹配方式和优先级排序均实现；P0 立即停止 AI 并转人工；门禁检查三类关键词齐全 | `tests/robotConfig.test.js`、`tests/botsRequirements.test.js` |
| 7 | 每机器人 3-5 类真人消息模板 | 欢迎、兜底、转人工、引导、反馈模板可配置；门禁要求至少 3 类；每条均经过真人话术检查 | `tests/robotConfig.test.js` |
| 8 | 6 个标准推送位、限频、时段、考前静默、10/50/100 灰度、自定义位 | 标准 6 位默认生成；单机器人/学员每日 1 条、跨机器人每日 3 条、9:00-21:00、考前 3 天静默；稳定哈希灰度；自定义 schedule 可配置 | `tests/botPush.test.js`、`tests/botsRequirements.test.js` |
| 9 | cron/event/manual 定时任务、模板、圈选和启停 | 支持标准 5 字段 cron、事件触发和手动触发；模板 ID、全体/阶段/院校/订阅圈选、启用状态进入配置和实际投递流程 | `tests/botPush.test.js` |
| 10 | 六个核心转人工词；停止 AI、转坐席、写工单、通知班主任 | 默认要求“人工/老师/真人/客服/转人工/找老师”；命中后不再生成 AI 回复，创建 P0 工单、违规事件与员工站内通知；企微人工消息和接管事件也会取消排队回复 | `tests/botsRequirements.test.js`、`tests/wecomKfDispatcher.test.js` |
| 11 | 入口机器人按学科/择校/任务引导分流 | 支持 `routing` 规则把数学、英语、政治、专业课、择校、任务/打卡分配到目标机器人；非入口机器人可自行处理 | `src/services/configuredBotRuntime.js`、`tests/botsRequirements.test.js` |
| 12 | 150 字内兜底，不说“不知道”，必须给引导 | 上线门禁检查长度和禁用表达；运行时无匹配时使用该兜底，并保留可继续提问的方向 | `tests/robotConfig.test.js` |
| 13 | 创建流程检查清单；缺项不得上线；新建默认草稿 | 机器人新建状态为 `draft`；上线接口运行完整检查；缺任一关键项返回 409；通过后才进入 `online`，所有操作写审计 | `tests/botsRequirements.test.js`、`tests/databaseMigration.test.js` |
| 14 | 真人话术 5 关自检 | 独立检查器覆盖廉价称呼、空泛鼓励、emoji、结构化标签和非口语长句，并用于 Prompt、模板和兜底校验 | `tests/robotConfig.test.js` |
| 15 | 内容、兜底、禁用词是上线关键项 | 三项均为强制门禁；不完整机器人在界面展示未通过项，无法上线 | `tests/robotConfig.test.js`、`tests/botsRequirements.test.js` |

## 手册补充能力

手册及 V5/V9 原型涉及的实际企微运行能力也已接入：

| 能力 | 落地内容 | 证据 |
| --- | --- | --- |
| 企业微信群 | 管理员读取通讯录、建群、配置默认机器人/全量回复/消息合并等待、角色分配；非管理员拒绝 | `tests/wecomAdmin.test.js` |
| 微信客服（普通微信） | 客服账号同步、回调验签解密、分页 cursor、客户列表、人工/AI 接管、2048 字节与最多 5 条拆分 | `tests/wecomKf.test.js`、`tests/wecomKfDispatcher.test.js` |
| 连续消息合并 | 同一客户文字、图片、语音、文件在最后一条后等待并只生成一次正式回复；重复 msgid 幂等 | `tests/wecomKfDispatcher.test.js` |
| 图片题目 | 私有文件转 base64，MiniMax-M3 使用高精度多模态配置；拒绝非图片和不支持格式 | `tests/minimaxMultimodal.test.js` |
| 语音 | 企业微信音频转换后调用火山引擎 Seed-ASR 2.0 高精度协议；限制时长，区分可重试和确定性失败 | `tests/audioTranscription.test.js` |
| 时效与官方答案 | MiniMax 联网工具调用、官方来源约束、DeepSeek 最大思考复核、无效引用和高风险招生承诺拦截 | `tests/minimaxWebSearch.test.js`、`tests/officialAnswerEngine.test.js` |
| 会话存档 | 优先维护版企微会话存档 SDK，解密缺口不推进 cursor | `tests/wecomArchive.test.js` |
| 小程序订阅提醒 | 官方字段构造、本人授权/读取隔离、失败状态和失效订阅记录 | `tests/wxSubscribe.test.js` |
| 运营可观测 | 上线检查、P0 工单、违规次数、灰度批次、机器人配置审计；审计弹窗显示正确标题 | `tests/botsRequirements.test.js`、`tests/robotConfig.test.js` |

## 表 1-5 字段验收

| 表 | 原始字段 | 数据与界面映射 |
| --- | --- | --- |
| 表 1 | 姓名、性别、高考/四级/专升本英语、词汇量、既往复习、英语弱项、高考/专升本数学、英语一/二、数学一/二、收货信息、要求/自评、预留 | `student_profiles` 完整保存；收货信息拆为姓名/电话/地址；另补目标院校、专业、阶段、班主任和周报邮箱；预留内容写 `extra_json` |
| 表 2 | 目标院校、英语/政治/业务课 1/业务课 2 目标分、英语长期/阶段、数学、政治、专业、额外任务 | `student_plan_templates` 一一映射；管理后台可展开编辑；半自动和全自动生成均读取该模板 |
| 表 3 | 序号、姓名、英语/数学/政治/专业课完成情况 | 晚间消息按当日任务编号展示；支持“1完成 2未完成”等回复；完成状态和反馈持久化并驱动次日计划 |
| 表 4 | 序号、拼写、中文意思、助记 | 二进制 `.xls` 识别后绕过文本编码误判；导入 `flashcards`；学生可复习并按同一卡片 UPSERT 记录；同一文件也可作为知识库文档处理 |
| 表 5 | 题目、A-D 选项、助记（原样例无数据行） | 完整行导入政治选择题；选项、正确答案和助记/解析进入学生列表与详情 API；空模板安全返回 0 导入 |

## V5/V9 原型融合

原型中的机器人列表、角色视图、关键词、模板、Prompt、推送、定时任务、工单、配置审计、灰度发布以及管理员/运营/客服视角，没有以静态样例重新复制，而是融合为现有管理后台的真实数据界面：

- “机器人角色”提供创建、搜索、配置、上线门禁、暂停和手动推送。
- “企业微信群”和“微信客服（普通微信）”分别管理两种渠道及角色分配。
- “学员档案与计划”承载表 1、表 2、表 3 闭环。
- “工单 / 审计 / 灰度”展示在线数、完整度、P0、违规和灰度批次。
- 每个机器人可查看对话记录和配置审计；创建、更新、上线、推送、灰度等动作均有审计记录。
- 移动端后台采用紧凑顶栏和横向菜单，当前入口自动居中，避免原侧栏占满首屏。

## 浏览器证据

| 证据 | 文件 |
| --- | --- |
| 运营面板桌面端 | `output/playwright/admin-operations-desktop.png` |
| 机器人后台移动端 | `output/playwright/admin-robots-mobile.png` |
| 表 2 模板桌面端 | `output/playwright/admin-plan-template-desktop.png` |
| 公开登记页桌面端 | `output/playwright/student-profile-desktop.png` |
| 公开登记页移动端 | `output/playwright/student-profile-mobile.png` |
| 知识库处理结果 | `output/playwright/knowledge-base-processed-desktop.png` |
| 人工推送、灰度、登记、计划、导入和审计语义快照 | `output/playwright/*.yml` |

浏览器实测完成了以下真实 UI 操作：创建并上线 `R-01 / browser_bot`、手动推送给学生 2（发送 1，跳过 0）、启动 50% 灰度、读取审计、生成并提交登记链接、保存表 2、分别生成半自动和全自动次日 6 项计划、上传并处理原始 `表4.xls`、使用表 4/表 5 专用入口导入、查看运营统计。所有相关请求均为 200。

## 关键实现入口

- 数据结构与迁移：`src/db.js`
- 表 1/2/3 与周计划 API：`src/routes/requirements.js`、`src/services/studyPlan.js`
- 机器人配置和运行时：`src/services/robotConfig.js`、`src/services/configuredBotRuntime.js`
- 主动推送与计划任务：`src/services/botPush.js`、`src/services/scheduler.js`
- 知识库与导入：`src/routes/knowledgeBase.js`、`src/services/knowledgeBase.js`、`src/routes/questions.js`
- 企微渠道：`src/routes/wecomAdmin.js`、`src/routes/wecomKf.js`、`src/services/wecomKfDispatcher.js`
- 管理界面：`public/admin.html`、`public/admin.js`
- 公开登记：`public/student-profile.html`、`public/student-profile.js`
- 小程序计划、词汇、通知：`miniprogram/pages/plan/`、`miniprogram/pages/vocabulary/`、`miniprogram/pages/notification/`
