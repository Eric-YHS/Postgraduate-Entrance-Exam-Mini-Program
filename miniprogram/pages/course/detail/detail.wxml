<view class="container detail-container">
  <!-- 加载中 -->
  <ky-loading wx:if="{{ loading }}" fullscreen="{{ true }}" />

  <!-- 错误 -->
  <ky-error wx:elif="{{ error }}" bind:retry="onRetry" />

  <!-- 课程详情 -->
  <block wx:elif="{{ detail }}">
    <!-- 课程封面 -->
    <view class="cover-section">
      <image class="course-cover" src="{{ detail.coverUrl }}" mode="aspectFill" />
      <view class="cover-mask">
        <view class="cover-badge">免费开放</view>
      </view>
    </view>

    <!-- 课程信息 -->
    <view class="info-card card">
      <view class="course-title">{{ detail.title }}</view>
      <view class="course-meta">
        <text class="meta-item">讲师：{{ detail.teacherName }}</text>
        <text class="meta-item">{{ detail.chapterCount }} 节</text>
      </view>
      <view class="course-desc">{{ detail.description }}</view>
    </view>

    <!-- 章节列表 -->
    <view class="chapter-card card">
      <view class="section-title">课程目录</view>
      <course-chapter-list
        course="{{ detail }}"
        progress-map="{{ progressMap }}"
        bind:videotap="onVideoTap"
      />
    </view>
  </block>

  <view wx:if="{{ detail }}" class="course-action-placeholder"></view>
  <view wx:if="{{ detail }}" class="course-action-bar">
    <view class="course-action-bar__content">
      <view class="course-action-bar__info">
        <text class="course-action-bar__title">免费开放</text>
        <text class="course-action-bar__subtitle">全部内容均可直接学习</text>
      </view>
      <view class="course-action-bar__button" bindtap="onStartLearning">开始学习</view>
    </view>
    <ky-safe-area position="bottom" />
  </view>
</view>
