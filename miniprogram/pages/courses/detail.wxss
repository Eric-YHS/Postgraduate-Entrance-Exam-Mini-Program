/* 课程详情页样式 */
.video-container {
  border-radius: 24rpx;
  overflow: hidden;
  background: #000;
  box-shadow: 0 16rpx 48rpx rgba(0, 0, 0, 0.15);
}

.video-player {
  width: 100%;
  height: 420rpx;
}
