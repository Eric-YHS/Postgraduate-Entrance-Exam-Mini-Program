<view class="chapter-list">
  <view class="chapter-item" wx:for="{{ displayChapters }}" wx:key="id" wx:for-item="chapter">
    <view class="chapter-header" bindtap="onChapterTap" data-id="{{ chapter.id }}">
      <view class="chapter-info">
        <text class="chapter-title">{{ chapter.title }}</text>
        <text class="chapter-count">{{ chapter.videos.length }} 节</text>
      </view>
      <view class="chapter-arrow {{ expandedChapters[chapter.id] ? 'chapter-arrow--up' : '' }}">></view>
    </view>

    <view class="chapter-videos {{ expandedChapters[chapter.id] ? 'chapter-videos--expanded' : '' }}">
      <view
        wx:for="{{ chapter.videos }}"
        wx:key="id"
        wx:for-item="video"
        class="video-item"
        bindtap="onVideoTap"
        data-video-id="{{ video.id }}"
        data-chapter-id="{{ chapter.id }}"
      >
        <view class="video-icon">
          <text>▶</text>
        </view>

        <view class="video-info">
          <view class="video-title-row">
            <text class="video-title">{{ video.title }}</text>
          </view>

          <view class="video-meta">
            <text class="video-duration">{{ video.durationText }}</text>
            <view class="video-progress" wx:if="{{ video.showProgress }}">
              <view class="video-progress__bar">
                <view class="video-progress__fill" style="width: {{ video.progressPercent }}%;"></view>
              </view>
              <text class="video-progress__text">已学 {{ video.progressPercent }}%</text>
            </view>
            <text class="video-completed" wx:if="{{ video.completed }}">已完成</text>
          </view>
        </view>
      </view>
    </view>
  </view>
</view>
