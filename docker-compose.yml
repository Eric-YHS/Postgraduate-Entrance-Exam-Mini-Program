version: "3.9"

services:
  study-planner:
    build: .
    container_name: study-planner
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    environment:
      - TZ=Asia/Shanghai
